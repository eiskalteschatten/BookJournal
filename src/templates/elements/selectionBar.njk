{% macro create(selectors, fieldId, defaultValue = "") %}
    <input type="hidden" id="{{ fieldId }}" value="{{ defaultValue }}">

    <div class="selection-bar">
        {% for value, name in selectors %}
            <div
                class="selector {% if value == defaultValue %}selected{% endif %}"
                data-value="{{ value }}"
            >
                {{ name }}
            </div>
        {% endfor %}
    </div>
{% endmacro %}
