<ul class="list">
    {% for element in listElements %}
        {% set id = element.id %}
        {% set classes = element.classes %}
        {% set iconPath = element.iconPath %}
        {% set displayName = element.displayName %}
        {% set dataFields = element.dataFields %}

        {% include "src/templates/listElement.njk" %}
    {% endfor %}
</ul>
