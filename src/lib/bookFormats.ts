export interface BookFormats {
  paperback: string;
  hardback: string;
  ebook: string;
  audiobook: string;
  other: string;
}

export default {
  paperback: 'Paperback',
  hardback: 'Hardback',
  ebook: 'E-Book',
  audiobook: 'Audiobook',
  other: 'Other',
} as BookFormats;
